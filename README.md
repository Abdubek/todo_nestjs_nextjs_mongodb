## Getting Started

First, run the docker-compose:

```bash
docker-compose up -d
```

and start backend:

```bash
cd backend & npm run start:dev
```

start frontend:

```bash
cd frontend & npm run dev
```
